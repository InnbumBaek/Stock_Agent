# 주가 모니터링 — Claude Code 안내

한국거래소·금융감독원 공식 API 로 상장 포트폴리오사의 숫자를 재고, 그것으로
**회수 판단 리포트**를 만드는 파이썬 도구다. 최종 결정은 오프라인 회의에서
사람이 한다.

```
stock-monitor/    ki_monitor.py 한 파일이 전부다 (수집 · 계산 · 리포트)
agents/           판단층 — 측정층을 감싸기만 하고 손대지 않는다
docs/             실행 안내 · 네트워크 요청서 · 논문 수확기 · 감사 스크립트
RUN_ALL.cmd       환경 점검 → API 진단 → 원장 → 논문 → 리포트
SCHEDULE.cmd      평일 자동 실행 등록 (07:30 · 08:50 · 16:10)
```

`stock-monitor/` 는 **잰다.** `agents/` 는 그것을 읽어 **문장으로 만들고**, 그
문장이 나가도 되는지 검사한다. 두 층 사이의 관문이 이 저장소의 요점이다
(`agents/README.md`).

데스크 9개와 규칙 5종의 정의는 `.claude/agents/` · `.claude/skills/` 에 있다.
**그 정의 파일은 테스트가 돌려 보지 않는다** — 데스크 이름을 코드에서 바꾸고
`.md` 를 안 고치면 트리거는 있는데 아무도 오지 않는 소집이 생기고, 그것은 오류
한 줄 없이 "조용한 날"처럼 보인다. `docs/audit.py` 가 대조한다.

---

## 절대 깨지 말아야 할 것

### 1. 재기만 하고 판단하지 마라

`ki_monitor.py` 의 계산 코드에 점수·등급·매매 시그널을 넣지 마라. 이 도구가
내는 것은 **측정값과 그 가정·한계**다. 회의에서 근거 없는 결론이 근거 있는
숫자처럼 읽히는 것이 이 시스템의 가장 조용한 실패다.

`selftest` 가 `facts` 출력에 판정 어휘가 없는지 검사한다.

### 2. 원장(`ki.sqlite`)에는 측정한 사실만 쓴다

공식 API 로 받은 값만 담는 장부다. 추정·가정·가상 포지션을 여기에 쓰면 다음번
측정이 오염되고, 그 오염은 되돌릴 수 없다. **KIS 실시간 스냅샷도 마찬가지다** —
`CATALOG` 의 `kis.snap.*` 은 `persist=False` 다. 장중 값이 일봉 장부에 섞이면
같은 오염이다.

### 3. 없는 값을 만들지 마라

못 구한 값은 `None` 이고 사유를 함께 낸다. 0 으로 채우지 않는다. 원장은
**일별 종가**라 며칠 묵을 수 있으므로, 기준일과 경과일수를 항상 붙인다.
`catchup` 이 밀린 영업일을 채우고 며칠 뒤처졌는지 화면에 적는다.

### 4. 재료에는 등급이 있다 — 섞지 마라

**1차** — KRX · DART · 한국은행 ECOS · KIS. 근거로 삼아도 된다.
**참고** — 뉴스 헤드라인 · 심리지표. "이런 이야기가 돈다"까지만.
**방법론** — 논문. 계산 규칙의 출처이지 시세 출처가 아니다.
**사내** — `exit_plan.csv` 같이 우리가 넣은 값.

리포트는 절마다 하단에 이 등급과 함께 출처를 적는다(`SECTION_SOURCES`).
새 재료를 붙일 때는 **어느 등급인지 먼저 정하고** 그 절의 출처 줄에 넣어라.

**논문은 `.papers.json` 에 있는 것만 인용할 수 있다.** 팩터 계산은 그 논문 키를
달고 나가고, `selftest` 가 "모든 팩터가 실재하는 논문을 인용하는가"를 검사한다.
`docs/fetch_papers.py --harvest` 가 네 곳(arXiv q-fin · Semantic Scholar ·
OpenAlex · Crossref)에서 후보를 쌓고, `--adopt` 이 발행 정보를 Crossref 로 다시
대조한 것만 하루 한 편까지 채택본으로 옮긴다. 대조에 실패하면 넣지 않는다.

팩터 값과 함께 **그 논문이 주장하지 않는 것**(`limits`)을 반드시 같이 낸다.
가장 흔한 사고는 논문이 말한 적 없는 것을 말했다고 읽는 것이다.

**논문에는 상태가 있다** (`ki.papers/2`). `unverified` 는 사람이 골랐으나 우리
표본으로 계산해 본 적이 없는 것이고, `adopted` 는 재현된 것, `warned` 는
흔들렸거나 재검 기한이 지난 것, `retired` 는 우리 표본에서 성립하지 않는 것이다.
인용이 막히는 것은 `retired` 뿐이고, 나머지는 **표시를 달고** 나간다.

은퇴본도 지우지 않는다. "이 논문은 코스닥에서 성립하지 않는다"도 측정 결과이고,
지우면 다음 사람이 같은 논문을 다시 주워 온다.

이 도구 자신의 계산 규칙이 근거로 삼는 논문(재현 임계·표준오차 추정량)은
`method_papers` 에 따로 둔다. 팩터 출처와 섞으면 "모든 팩터가 실재하는 논문을
인용하는가" 검사가 팩터와 상관없는 논문까지 세게 되어 뜻이 없어진다.

### 4-b. 봉투는 자기가 읽은 것을 적는다

원장은 `INSERT OR REPLACE` 로 덮어써진다. DART 정정공시가 오면 같은 칸이
조용히 바뀌고 옛 값은 남지 않는다. 석 달 뒤에 같은 질문을 다시 던지면 다른
답이 나오는데, 그때 **"에이전트가 틀렸다"와 "데이터가 바뀌었다"를 구분할 수
없다.** 자기 이력을 설명하지 못하는 시스템이 된다.

원장을 양방향 시간 장부로 바꾸는 것은 측정층을 건드리는 큰 공사다. 그래서
**봉투 쪽에서 푼다** — 값을 낸 봉투는 `read` 에 무엇을 읽었는지(출처·키·값·
기준일) 적는다. 지금 원장과 대조하면 어느 쪽이 움직였는지가 바로 나온다
(`agents/replay.py`).

못 읽은 값도 `null` 로 적는다. 빼 버리면 "안 읽었다"와 "읽었는데 없더라"가
구분되지 않는다.

### 5. 대외비·자격증명을 저장소에 넣지 마라

`ki.sqlite` · `watchlist.csv` · `exit_plan.csv` · `positions.csv` · `out/` ·
`logs/` 는 `.gitignore` 에 있다. 저작권이 아니라 **영업비밀·미공개중요정보**
문제다.

**키는 `.gitignore` 에 기대지 않는다 — 저장소 밖에 둔다.**

```
%USERPROFILE%\Stock-Agent-keys\.env      기본값 (STOCK_AGENT_KEYS 로 덮어쓴다)
```

`.gitignore` 는 약하다. `git add -f` 한 번이면 뚫린다. 저장소 밖이면 실수로
커밋할 경로 자체가 없다. 옮기는 것은 **키뿐이다** — 원장·워치리스트·포지션·
리포트는 원래 자리 그대로다.

**오류 문구에 키가 남지 않게 해라.** `requests` 예외에는 호출 URL 이 통째로
들어가고 DART 는 인증키를 쿼리 파라미터로 보낸다. 사람에게 보이는 예외는 전부
`scrub()` 을 통과시켜라. `selftest` 가 검사한다.

**HTTPS 가 막혀도 키를 평문으로 내보내지 않는다.** 평문 HTTP 폴백은 기본으로
꺼져 있고, `KRX_ALLOW_PLAINTEXT=1` 로 알고서 켤 때만 쓴다.

**`.env` 변종을 이름으로 막아라.** `.gitignore` 의 `.env` 규칙은 `.env` 라는
이름 하나만 막는다. `env.txt` 는 그 어떤 규칙에도 걸리지 않은 채 저장소에
들어와 있었다 — 키 6개가 평문으로. 키를 담을 수 있는 이름은 확장자까지
막는다(`env.txt` · `env*.txt` · `*.env.txt`, 하위 디렉터리 포함). 그리고 이건
2차 방어선일 뿐이다. 1차는 위에 쓴 대로 **저장소 밖**이다.

### 6. 리포트에 들어가는 문자열은 이스케이프한다

`_esc()` 를 거치지 않은 문자열을 HTML 에 넣지 마라. `selftest` 가 `<script>`
주입을 검사한다.

### 7. 줄바꿈을 바꾸지 마라

`ki_monitor.py` 는 **CRLF** 파일이고 `docs/*.py` 는 LF 다. 편집 도구가 이걸
바꾸면 파일 전체가 diff 에 잡혀 실제 변경이 묻힌다. 편집 후 `file <파일>` 로
확인해라.

### 8. 배치 파일은 눈으로 검증할 수 없다

이 환경에서는 `cmd.exe` 를 실행해 볼 수 없다. 괄호 블록 안의 라벨, 없는
라벨로의 `goto`, 지연확장에서 짝 없는 `!` — 셋 다 **오류 한 줄 없이** 창만
닫힌다. 배치를 고쳤으면 반드시 `docs/audit.py` 를 돌려라. 라벨을 찾을 때는
`call :이름` 이 먼저 걸리지 않도록 **줄 단위로** 찾아라.

### 9. 산출물은 하나로 유지한다

같은 목적의 문서를 두 벌 만들지 마라. 계획안 덱은 한 파일이고, 곁가지 요구가
들어오면 **그 파일 안에서** 절을 고친다. 두 번째 파일을 만드는 순간 어느 쪽이
최신인지 아무도 모르게 되고, 회의에는 반드시 낡은 쪽이 올라간다.

빌드 스크립트의 출력 파일명도 저장소에 커밋된 그 이름이어야 한다. 스크래치
이름(`deck2.pptx` 같은)으로 떨어지면 빌드가 산출물을 갱신하지 않는다.

### 10. 제도 수치는 유통기한이 있다

리픽싱 하한 70%, 코스닥벤처펀드 편입비율 15%·35%, 공모주 우선배정 비율,
소득공제 한도 — 이런 값은 코드의 상수가 아니라 **그때의 제도**다. 법령·규정
개정으로 조용히 틀린 값이 된다.

제도에서 온 수치는 **확인 날짜와 근거(법령·규정 조문 또는 공식 보도자료)를
함께** 적는다. 날짜 없는 제도 수치는 근거가 아니다. 인용하기 전에 원문을 다시
본다.

---

## 검증

```bash
cd stock-monitor && python ki_monitor.py selftest   # 128개 (키·네트워크 불필요)
python agents/selftest.py                           # 172개 (판단층)
python docs/audit.py                                # 저장소 전반
```

원격 세션(Claude Code on the web)은 컨테이너가 매번 새로 뜬다.
`.claude/hooks/session-start.sh` 가 위 두 명령이 바로 돌 만큼은 깔아 둔다 —
`pandas numpy scipy requests jinja2 lxml defusedxml`, 덱을 눈으로 확인할
`libreoffice-impress poppler-utils fonts-noto-cjk`, 그리고 `pptxgenjs`.
여러 번 돌려도 같고, 이미 있으면 건너뛴다. 의존성이 늘면 이 훅에 넣어라.

`docs/audit.py` 는 테스트가 못 보는 것을 본다 — 줄바꿈이 뒤집혔는지, 배치 파일의
괄호·라벨·`for /f` 인용이 성립하는지, 저장소에 키·실명이 새지 않았는지, 문서의
테스트 개수가 실제와 맞는지.

종단 확인:

```bash
cd stock-monitor
python ki_monitor.py facts --code 000660 --indent 2
python ki_monitor.py report --market KOSDAQ
```
