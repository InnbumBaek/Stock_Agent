# 주가 모니터링 — Claude Code 안내

한국거래소·금융감독원 공식 API 로 상장 포트폴리오사의 숫자를 재고, 그것으로
**회수 판단 리포트**를 만드는 파이썬 도구다. 최종 결정은 오프라인 회의에서
사람이 한다.

```
stock-monitor/    ki_monitor.py 한 파일이 전부다 (수집 · 계산 · 리포트)
agents/           판단층 — 측정층을 감싸기만 하고 손대지 않는다
docs/             실행 안내 · 네트워크 요청서 · 논문 수확기 · 감사 스크립트
RUN_ALL.cmd       환경 점검 → API 진단 → 원장 → 논문 → 리포트
SCHEDULE.cmd      평일 자동 실행 등록 (07:30 · 08:50 · 16:10)
```

`stock-monitor/` 는 **잰다.** `agents/` 는 그것을 읽어 **문장으로 만들고**, 그
문장이 나가도 되는지 검사한다. 두 층 사이의 관문이 이 저장소의 요점이다
(`agents/README.md`).

데스크 9개와 규칙 5종의 정의는 `.claude/agents/` · `.claude/skills/` 에 있다.
**그 정의 파일은 테스트가 돌려 보지 않는다** — 데스크 이름을 코드에서 바꾸고
`.md` 를 안 고치면 트리거는 있는데 아무도 오지 않는 소집이 생기고, 그것은 오류
한 줄 없이 "조용한 날"처럼 보인다. `docs/audit.py` 가 대조한다.

---

## 절대 깨지 말아야 할 것

### 1. 재기만 하고 판단하지 마라

`ki_monitor.py` 의 계산 코드에 점수·등급·매매 시그널을 넣지 마라. 이 도구가
내는 것은 **측정값과 그 가정·한계**다. 회의에서 근거 없는 결론이 근거 있는
숫자처럼 읽히는 것이 이 시스템의 가장 조용한 실패다.

`selftest` 가 `facts` 출력에 판정 어휘가 없는지 검사한다.

### 2. 원장(`ki.sqlite`)에는 측정한 사실만 쓴다

공식 API 로 받은 값만 담는 장부다. 추정·가정·가상 포지션을 여기에 쓰면 다음번
측정이 오염되고, 그 오염은 되돌릴 수 없다. **KIS 실시간 스냅샷도 마찬가지다** —
`CATALOG` 의 `kis.snap.*` 은 `persist=False` 다. 장중 값이 일봉 장부에 섞이면
같은 오염이다.

### 3. 없는 값을 만들지 마라

못 구한 값은 `None` 이고 사유를 함께 낸다. 0 으로 채우지 않는다. 원장은
**일별 종가**라 며칠 묵을 수 있으므로, 기준일과 경과일수를 항상 붙인다.
`catchup` 이 밀린 영업일을 채우고 며칠 뒤처졌는지 화면에 적는다.

### 4. 재료에는 등급이 있다 — 섞지 마라

**1차** — KRX · DART · 한국은행 ECOS · KIS. 근거로 삼아도 된다.
**참고** — 뉴스 헤드라인 · 심리지표. "이런 이야기가 돈다"까지만.
**방법론** — 논문. 계산 규칙의 출처이지 시세 출처가 아니다.
**사내** — `exit_plan.csv` 같이 우리가 넣은 값.

리포트는 절마다 하단에 이 등급과 함께 출처를 적는다(`SECTION_SOURCES`).
새 재료를 붙일 때는 **어느 등급인지 먼저 정하고** 그 절의 출처 줄에 넣어라.

**논문은 `.papers.json` 에 있는 것만 인용할 수 있다.** 팩터 계산은 그 논문 키를
달고 나가고, `selftest` 가 "모든 팩터가 실재하는 논문을 인용하는가"를 검사한다.
`docs/fetch_papers.py --harvest` 가 네 곳(arXiv q-fin · Semantic Scholar ·
OpenAlex · Crossref)에서 후보를 쌓고, `--adopt` 이 발행 정보를 Crossref 로 다시
대조한 것만 하루 한 편까지 채택본으로 옮긴다. 대조에 실패하면 넣지 않는다.

팩터 값과 함께 **그 논문이 주장하지 않는 것**(`limits`)을 반드시 같이 낸다.
가장 흔한 사고는 논문이 말한 적 없는 것을 말했다고 읽는 것이다.

**논문에는 상태가 있다** (`ki.papers/2`). `unverified` 는 사람이 골랐으나 우리
표본으로 계산해 본 적이 없는 것이고, `adopted` 는 재현된 것, `warned` 는
흔들렸거나 재검 기한이 지난 것, `retired` 는 우리 표본에서 성립하지 않는 것이다.
인용이 막히는 것은 `retired` 뿐이고, 나머지는 **표시를 달고** 나간다.

은퇴본도 지우지 않는다. "이 논문은 코스닥에서 성립하지 않는다"도 측정 결과이고,
지우면 다음 사람이 같은 논문을 다시 주워 온다.

이 도구 자신의 계산 규칙이 근거로 삼는 논문(재현 임계·표준오차 추정량)은
`method_papers` 에 따로 둔다. 팩터 출처와 섞으면 "모든 팩터가 실재하는 논문을
인용하는가" 검사가 팩터와 상관없는 논문까지 세게 되어 뜻이 없어진다.

### 4-b. 봉투는 자기가 읽은 것을 적는다

원장은 `INSERT OR REPLACE` 로 덮어써진다. DART 정정공시가 오면 같은 칸이
조용히 바뀌고 옛 값은 남지 않는다. 석 달 뒤에 같은 질문을 다시 던지면 다른
답이 나오는데, 그때 **"에이전트가 틀렸다"와 "데이터가 바뀌었다"를 구분할 수
없다.** 자기 이력을 설명하지 못하는 시스템이 된다.

원장을 양방향 시간 장부로 바꾸는 것은 측정층을 건드리는 큰 공사다. 그래서
**봉투 쪽에서 푼다** — 값을 낸 봉투는 `read` 에 무엇을 읽었는지(출처·키·값·
기준일) 적는다. 지금 원장과 대조하면 어느 쪽이 움직였는지가 바로 나온다
(`agents/replay.py`).

못 읽은 값도 `null` 로 적는다. 빼 버리면 "안 읽었다"와 "읽었는데 없더라"가
구분되지 않는다.

**원본(`raw`)과 파생(`derived`)을 구분한다.** 60일 평균 거래대금을 원장의
'거래대금' 칸과 대조하면 아무것도 안 바뀌었는데 "데이터가 바뀜"이 나온다.
거짓 '바뀜' 은 거짓 '일치' 만큼 나쁘다 — 회의에 자료가 움직였다고 알린다.
계산한 값은 `kind:"derived"` 로 적고 `basis` 에 계산 방법을 남긴다.

### 4-c. 갈렸다는 사실이 가장 중요한 정보다

데스크는 서로의 산출을 보지 않는다. 수렴하면 교차검증의 뜻이 사라지기
때문이다. 그런데 **격리만 해 놓고 아무도 대조하지 않으면 격리가 사 주는 것이
없다** — 서로 무관한 문장이 나올 뿐이고, 회의에는 그중 하나만 올라간다.

`agents/reconcile.py` 가 갈린 곳을 기계적으로 찾는다. 같은 칸을 다르게
읽었는가(원장은 하나다), 같은 논문을 다른 상태로 인용했는가(장부는 하나다),
같은 것을 쟀는데 값이 벌어졌는가. 갈리면 **양쪽 다** 한 급 위로 올라간다 —
한쪽만 올리면 그 선택 자체가 판정이 된다.

값을 낸 봉투에는 `measure`(잰 대상의 이름)를 붙인다. 없으면 대조에서 빠지고,
그때 "갈린 곳 없음"은 합의가 아니라 **안 봤다**는 뜻이다. 그 건수를 따로
센다.

문장의 뜻이 어긋나는 것은 코드가 못 잡는다. 의미 판단이고, 코드가 하면 조용히
틀린다. 그 몫은 `ic-chair` 와 회의다.

### 5. 대외비·자격증명을 저장소에 넣지 마라

`ki.sqlite` · `watchlist.csv` · `exit_plan.csv` · `positions.csv` · `out/` ·
`logs/` 는 `.gitignore` 에 있다. 저작권이 아니라 **영업비밀·미공개중요정보**
문제다.

**키는 `.gitignore` 에 기대지 않는다 — 저장소 밖에 둔다.**

```
%USERPROFILE%\Stock-Agent-keys\.env      기본값 (STOCK_AGENT_KEYS 로 덮어쓴다)
```

`.gitignore` 는 약하다. `git add -f` 한 번이면 뚫린다. 저장소 밖이면 실수로
커밋할 경로 자체가 없다. 옮기는 것은 **키뿐이다** — 원장·워치리스트·포지션·
리포트는 원래 자리 그대로다.

**오류 문구에 키가 남지 않게 해라.** `requests` 예외에는 호출 URL 이 통째로
들어가고 DART 는 인증키를 쿼리 파라미터로 보낸다. 사람에게 보이는 예외는 전부
`scrub()` 을 통과시켜라. `selftest` 가 검사한다.

**HTTPS 가 막혀도 키를 평문으로 내보내지 않는다.** 평문 HTTP 폴백은 기본으로
꺼져 있고, `KRX_ALLOW_PLAINTEXT=1` 로 알고서 켤 때만 쓴다.

**`.env` 변종을 이름으로 막아라.** `.gitignore` 의 `.env` 규칙은 `.env` 라는
이름 하나만 막는다. `env.txt` 는 그 어떤 규칙에도 걸리지 않은 채 저장소에
들어와 있었다 — 키 6개가 평문으로. 키를 담을 수 있는 이름은 확장자까지
막는다(`env.txt` · `env*.txt` · `*.env.txt`, 하위 디렉터리 포함). 그리고 이건
2차 방어선일 뿐이다. 1차는 위에 쓴 대로 **저장소 밖**이다.

### 6. 리포트에 들어가는 문자열은 이스케이프한다

`_esc()` 를 거치지 않은 문자열을 HTML 에 넣지 마라. `selftest` 가 `<script>`
주입을 검사한다.

### 7. 줄바꿈을 바꾸지 마라

`ki_monitor.py` 는 **CRLF** 파일이고 `docs/*.py` 는 LF 다. 편집 도구가 이걸
바꾸면 파일 전체가 diff 에 잡혀 실제 변경이 묻힌다. 편집 후 `file <파일>` 로
확인해라.

### 8. 배치 파일은 눈으로 검증할 수 없다

이 환경에서는 `cmd.exe` 를 실행해 볼 수 없다. 괄호 블록 안의 라벨, 없는
라벨로의 `goto`, 지연확장에서 짝 없는 `!` — 셋 다 **오류 한 줄 없이** 창만
닫힌다. 배치를 고쳤으면 반드시 `docs/audit.py` 를 돌려라. 라벨을 찾을 때는
`call :이름` 이 먼저 걸리지 않도록 **줄 단위로** 찾아라.

### 9. 산출물은 하나로 유지한다

같은 목적의 문서를 두 벌 만들지 마라. 계획안 덱은 한 파일이고, 곁가지 요구가
들어오면 **그 파일 안에서** 절을 고친다. 두 번째 파일을 만드는 순간 어느 쪽이
최신인지 아무도 모르게 되고, 회의에는 반드시 낡은 쪽이 올라간다.

빌드 스크립트의 출력 파일명도 저장소에 커밋된 그 이름이어야 한다. 스크래치
이름(`deck2.pptx` 같은)으로 떨어지면 빌드가 산출물을 갱신하지 않는다.

### 10. 제도 수치는 유통기한이 있다

리픽싱 하한 70%, 코스닥벤처펀드 편입비율 15%·35%, 공모주 우선배정 비율,
소득공제 한도 — 이런 값은 코드의 상수가 아니라 **그때의 제도**다. 법령·규정
개정으로 조용히 틀린 값이 된다.

제도에서 온 수치는 **확인 날짜와 근거(법령·규정 조문 또는 공식 보도자료)를
함께** 적는다. 날짜 없는 제도 수치는 근거가 아니다. 인용하기 전에 원문을 다시
본다.

### 11. 기능을 넣었으면 검토를 돌려라

`selftest` 가 전부 통과해도 기능이 안 돌 수 있다. 실제로 그랬다.

"게이트에 두 번 연속 반려되면 승격" 을 넣고 검사 232개가 전부 통과했는데,
그 규칙은 **한 번도 발동하지 않았다.** 작업지시서가 `attempt=2` 를 줘도 봉투가
그것을 적을 방법이 없어서(`make()` 가 1 로 고정), 같은 급에서 영원히 다시
돌고 있었다. 같은 검토에서 `method=null` 봉투 하나가 승격 단계 전체를 죽이는
것과, 단위가 다른 두 값(25 `business_days` vs 25 `hours`)을 "대조했고 일치"
라고 내는 것도 나왔다.

**내가 쓴 검사는 내가 떠올린 경로만 본다.** 안 떠올린 경로는 검사에 없고,
그래서 통과한다.

```bash
/code-review agents/ high
```

기능을 넣거나 고쳤으면 커밋 전에 한 번 돌린다. 나온 것은 **직접 재현해 보고**
고친다 — 재현되지 않으면 고치지 않는다. 고친 뒤에는 그 경로를 검사에 남겨서,
같은 구멍이 두 번 생기지 않게 한다.

### 12. 원장의 말을 짐작하지 마라 — 합성 자료는 자기 자신을 검증한다

판단층 전체가 원장의 날짜를 `YYYY-MM-DD`, 시장·지수를 `"KOSDAQ"` 이라고
가정하고 만들어졌다. **검사도 그 가정대로 표를 만들어 240개가 전부 통과했다.**
표를 만드는 쪽과 읽는 쪽이 같은 가정을 쓰면 둘 다 틀려도 통과한다.

진짜 원장은 KRX 응답을 그대로 적는다.

```
price_daily.date · index_daily.date   "20260917"    KRX BAS_DD (대시 없음)
instruments.list_date                 "20170102"    KRX LIST_DD
disclosure.rcept_dt                   "20260108"    DART
macro_daily.date        "20260917" 또는 "2026-09-11 00:00:00"   KRX / FRED
index_name                            "코스닥"       KRX IDX_NM
price_daily.market                    "KOSDAQ"      krx_daily_prices 의 영문 키
instruments.market                    한글일 수 있다  KRX MKT_TP_NM
```

나왔을 결과는 두 가지고, 둘 다 오류를 한 줄도 내지 않는다.

**조용히 비는 쪽** — 논문 셋(`ahxz2006` · `ritter1991` · `fh2001`)과 MCP 의
`universe` · `staleness` · `index_series` 가 영원히 "원장에 없습니다" 를 낸다.
그 문장은 회의에서 **자료가 없다**로 읽힌다 — 코드가 틀렸다가 아니라.

**조용히 틀리는 쪽** — 이쪽이 더 나쁘다. 트리거의 7일 창은
`date > date(?, '-7 day')` 였다. `"20260911"` 과 `"2026-09-04"` 를 문자열로
비교하면 4번째 글자에서 `'0'` 과 `'-'` 이 만나 **같은 해 전체가 창 안으로
들어온다.** 재현해 보니 여덟 달 전 -40% 하락이 오늘의 소집으로 올라왔다.

그래서 `agents/dialect.py` 가 있다. 원장에 **물어본다.**

- 질의에 시장·지수 이름을 박지 마라. `resolve_market` · `resolve_index` 를 쓴다.
- SQLite 의 `date(...)` 산술에 기대지 마라. ISO 만 받는다. `sql_date()` 를 쓴다.
- 날짜를 직접 파싱하지 마라. `fromisoformat` 은 `"20170102"` 를 3.11 부터만
  받는다 — 3.10 에서는 전 종목이 조용히 빠진다. `parse_date()` 를 쓴다.
- **밖으로 내는 날짜는 ISO 다.** 봉투·대조·리포트가 전부 ISO 를 쓴다.
  `envelope.validate` 는 `asof` 가 정확히 `YYYY-MM-DD` 이기를 요구한다 —
  FRED 를 거친 `"2026-09-11 00:00:00"` 이 새면 게이트가 반려하는데, 사유가
  '날짜 형식'이라 원장이 원인이라는 것이 보이지 않는다.
- **`market` 은 표마다 다르다.** `price_daily` 는 영문(`KOSDAQ`)이고
  `instruments` 는 한글일 수 있다. 처음에 둘 다 한글이라고 단정했다가 리포트가
  "KOSDAQ 원장이 비어 있습니다" 로 죽었다 — **짐작이 틀렸다는 것을 짐작으로
  고치면 또 틀린다.** `resolve_market(con, hint, table=...)` 이 읽을 표에
  물어본다. 어느 쪽이든 코드가 아니라 원장이 답한다.
- **다른 시장의 지수로 대신하지 마라.** 유가증권 종목을 코스닥 지수에 대고
  재면 초과수익이 통째로 다른 값이 되는데 아무 표시도 붙지 않는다 — 판정까지
  정상으로 나온다. 시장별 기준 지수는 측정층의 `FACTS_BENCH` 를 가져다 쓴다.
- **원장이 비었다고 거절하지 마라.** `Denied` 는 '부를 수 없는 요청'이라는
  뜻이다. 도구마다 자기 사유 봉투가 있으니 그쪽이 말하게 둔다 — 프로토콜
  오류로 나가면 데스크가 사유를 읽지 못한다. 그리고 물어볼 표는 **그 도구가
  실제로 읽는 표**다(종목 목록은 `instruments`, 신선도는 `price_daily`).
- **합성 원장을 진짜 원장의 형식으로 만들어라.** 이것이 핵심이다. 코드만
  고치고 검사의 표를 그대로 두면 다음 사람이 같은 구멍을 다시 판다.

`docs/audit.py` [11] 이 질의 안의 영문 시장 이름과 `INSERT` 문의 ISO 날짜
문자열을 금지한다.

### 13. 시킨 것과 돌아온 것을 짝지어라

`convene` 은 작업지시서를 내고 `publish` 는 봉투를 받는다. 그 사이를 아무도
대조하지 않으면 셋이 구분되지 않는다.

**미이행** — 소집했는데 봉투가 안 왔다. 그 절이 빈다. 그리고 **빈칸은 리포트
에서 '조용한 날'과 똑같이 생겼다.** 반려는 반드시 묻게 되지만 빈칸은 아무도
묻지 않는다.
**무단** — 시킨 적 없는 봉투가 게이트만 통과하면 발행된다. 어느 소집에서
나왔는지 모르는 주장이라 되짚기도 안 된다.
**중복** — 한 지시서에 봉투가 둘. 값이 갈려도 회의는 하나만 본다.

`agents/dispatch.py` 가 `instance` 로 짝짓는다. 무단은 **빼고** 사유를 남기고,
중복은 **빼지 않고** 표시를 단다 — 둘 다 실제로 잰 값이라, 빼면 그 절이 통째로
비어 다시 빈칸이 된다.

**게이트 앞에서 봉투를 걸러 내지 마라.** 무단을 게이트 **전에** 빼도록 짰다가
검토에 잡혔다. 그러면 게이트 ⑥(자격증명 유출)이 그 봉투를 보지 못한다 —
DART 인증키가 든 무단 봉투 하나가 발행을 멈추지 못하고 통과했다. 그 기능을
넣기 전에는 멈췄다. **새 기능이 있던 보호를 껐다.**

발행하지 않는 것과 검사하지 않는 것은 다르다. 유출은 되돌릴 수 없어 게이트
⑥ 에 예외는 없다. 전부 걸고 **그 뒤에** 뺀다.

**봉투는 작업지시서가 준 `instance` 를 그대로 적는다.** 새로 만들면 지시서의
`...-07ba` 와 봉투의 `...-aa4a` 가 영원히 안 맞고, 회수 대조가 전부 '미이행 +
무단' 을 낸다. 데스크 정의 아홉 개에 규칙으로 들어 있고 `docs/audit.py` [10] 이
그 줄을 본다.

이것으로 **같은 구멍이 세 번째**다 — 작업지시서가 값을 주는데 봉투가 그것을
적을 방법이 없는 것. `attempt`(승격이 발동 안 함), `escalated_from`, 그리고
`instance`. 지시서에 새 칸을 넣으면 **봉투에 그 칸을 받는 인자가 있는지 먼저
확인해라.** 없으면 그 칸은 지시서에만 있고 아무 일도 하지 않는다.

**데스크를 돌리는 칸은 코드가 아니다.** `convene` → 지시서 → **[데스크]** →
봉투 → `publish` 에서 가운데는 사람이나 Claude Code 세션이 한다. 거기를 코드로
메우면 무엇을 넘겨줄지 고르는 일이 판단이 되고, 데스크의 격리가 형식만 남는다.
그래서 `publish` 는 배치에 걸지 않는다 — 걸면 매일 "미이행 N" 만 찍힌다.

### 14. 이것은 시장 감시기가 아니다 — 상장 포트폴리오사만 본다

이 도구는 **우리가 들고 있는 상장 포트폴리오사**의 회수 판단을 돕는다.
코스닥 전체는 그 판단의 배경일 뿐이다 (리포트 §7 '시장 배경').

측정층은 그 구분을 지킨다 — `_watchlist()` 가 주인공을 정하고, 시장 전체
행렬(`quant_frames`)은 배경으로만 쓴다. **판단층에는 그 구분이 없었다.**
트리거가 `price_daily` 를 통째로 훑어서 하루 ±8% 움직인 종목마다 데스크를
불렀다. 재 봤다 —

```
코스닥 1,700종목 · 일간 변동성 3.5%
  ±8% 이상 움직인 종목      37개
  그중 우리 포트폴리오사     2개
  낭비되는 인스턴스         70개 (종목당 q2-disposal + risk-officer)
```

95%가 남의 회사다. 그리고 낭비로 끝나지 않는다 — 데스크가 우리가 갖지도 않은
회사에 대해 봉투를 쓰고, 그 봉투가 게이트를 통과해 **회수 판단 리포트에
섞인다.**

`agents/scope.py` 가 범위를 정한다. 종목을 보는 스캐너(`scan_disclosures` ·
`scan_price_moves` · `scan_lockups`)는 `codes=` 로 좁힌다. **거시와 논문 재검은
좁히지 않는다** — 그쪽은 시장·장부 전체가 대상이 맞다.

**목록을 못 읽으면 소집하지 않고 사유를 낸다.** 전체로 넘기면 위의 일이 그대로
벌어지고, 빈 목록으로 넘기면 '조용한 날'이 되어 아무도 묻지 않는다. 둘 다
조용히 틀린다. 소집 산출의 `scope` 가 "범위를 못 정한 날"이라고 크게 적는다.

**목록 파싱은 측정층 것을 쓴다** (`ki_monitor._watchlist()`). 종목코드 6자리
채우기 · 주석 줄 · 비상장 판정이 전부 거기 있고, 여기서 다시 읽으면 두 규칙이
갈라진다.

**종목코드는 대외비다.** 범위 산출에는 **개수와 사유만** 싣는다.

`docs/audit.py` [12] 가 스캐너가 실제로 거르는지, 산출이 범위를 밝히는지,
종목코드가 새지 않는지를 본다 — 합성 원장에는 우리 종목만 들어 있어서
`codes=` 를 지워도 테스트는 전부 통과한다.

### 15. 주가 모니터링은 사건이 없어도 돈다

트리거는 **하루** 변동만 봤다 (`MOVE_PCT = 8.0`). 그래서 천천히 빠지는 종목이
통째로 안 보였다. 재 봤다 —

```
6개월 누적        -58.1%
최악의 하루       -3.6%      (임계 ±8.0%)
거래대금 20일평균  -79%
발동한 트리거      0건
```

반토막이 나고 유동성이 마르는 동안 데스크가 한 번도 안 불렸다. **그리고 이쪽이
회수 판단이 가장 필요한 경우다** — 하루에 8% 빠지는 날은 이미 늦은 날이고,
천천히 빠지는 구간은 아직 팔 수 있는 구간이다.

`agents/watch.py` 가 상장 포트폴리오사를 **매일** 잰다. 사건이 없어도 잰다.

```
누적 수익률   1주 · 1개월 · 3개월 · 6개월
낙폭          원장 구간 내 최고 종가 대비
52주 신저가   일 년 치가 없으면 그렇게 부르지 않는다
유동성        최근 20일 평균 거래대금 / 직전 60일 평균
```

그중 셋이 소집으로 이어진다 — `price.drift`(q1·q2) · `liquidity.dry`(q2·q3) ·
`price.low52`(q1). **표는 보라고 있고, 소집은 임계를 넘을 때만 한다.**

**임계는 사내 운영 기준이다** (규칙 4 의 '사내'). 논문도 제도도 아니고, 그렇게
표시해서 내보낸다. 근거는 하나씩 적는다 — 3개월 -25% 는 목표 회수액을 다시
계산해야 하는 크기이고, 유동성 0.5배는 처분 소요일수가 두 배가 된다는 뜻이다.
근거 없는 임계는 숫자가 아니라 취향이고, 취향은 회의에서 다투기만 한다.

**여기서도 재기만 한다** (규칙 1). 낙폭이 크다고 등급을 매기거나 결론으로
옮기지 않는다. 그 판단은 데스크가 하고 결정은 회의가 한다.

---

## 검증

```bash
cd stock-monitor && python ki_monitor.py selftest   # 128개 (키·네트워크 불필요)
python agents/selftest.py                           # 340개 (판단층)
python docs/audit.py                                # 저장소 전반
```

원격 세션(Claude Code on the web)은 컨테이너가 매번 새로 뜬다.
`.claude/hooks/session-start.sh` 가 위 두 명령이 바로 돌 만큼은 깔아 둔다 —
`pandas numpy scipy requests jinja2 lxml defusedxml`, 덱을 눈으로 확인할
`libreoffice-impress poppler-utils fonts-noto-cjk`, 그리고 `pptxgenjs`.
여러 번 돌려도 같고, 이미 있으면 건너뛴다. 의존성이 늘면 이 훅에 넣어라.

`docs/audit.py` 는 테스트가 못 보는 것을 본다 — 줄바꿈이 뒤집혔는지, 배치 파일의
괄호·라벨·`for /f` 인용이 성립하는지, 저장소에 키·실명이 새지 않았는지, 문서의
테스트 개수가 실제와 맞는지.

종단 확인:

```bash
cd stock-monitor
python ki_monitor.py facts --code 000660 --indent 2
python ki_monitor.py report --market KOSDAQ
```
