# 데이터 출처 및 이용 조건

이 도구가 사용하는 모든 외부 데이터의 출처와 저작권 주체입니다.

> 이 문서는 법률 자문이 아닙니다. 조직에서 업무 목적으로 사용하시려면
> 각 제공처의 이용약관을 직접 확인하십시오.

---

## 1. 사용하는 출처

| 출처 | 데이터 | 저작권 주체 | 인증 |
|---|---|---|---|
| KRX 정보데이터시스템 Open API | 종목 일별시세, 지수, 국고채, 채권지수, 선물 | 한국거래소 | 인증키 (서비스별 사용신청) |
| DART Open API | 공시목록, 기업개황, 주요계정 재무 | 금융감독원 | 인증키 |
| 한국은행 ECOS Open API | 100대 통계지표 (기준금리·환율·물가·성장률 등) | 한국은행 | 인증키 |
| 한국투자증권 KIS Open API | 국내주식 현재가·호가 (실시간, 선택) | 한국투자증권 | 앱키/시크릿 |
| FRED (공공 계열만) | 미국 국채 2년·10년, 장단기 스프레드, 달러지수 | Federal Reserve Board (미국 정부 저작물) | 무료 API 키 |

전부 **공공기관·중앙은행·거래소의 1차 출처**입니다. 2차 가공물(증권사 리포트,
언론 기사, 데이터 벤더)은 여기 없습니다.

ECOS 는 통계표코드를 하드코딩하지 않고 `KeyStatisticList`(100대 통계)를 씁니다 —
코드를 확인 없이 박아 두면 엉뚱한 계열을 '기준금리'라고 부르게 되기 때문입니다.
응답이 통계명·단위·시점을 들고 오므로 그대로 보존합니다.

KIS 는 **시세 조회 두 개만** 화이트리스트에 있습니다. 같은 서버에 주문 API 가
있지만 호출하지 않습니다 — `OrderNotAllowed` 로 막고 selftest 가 검사합니다.

KRX는 인증키 발급과 별개로 **서비스별 URL 사용신청**이 필요합니다.
신청하지 않은 서비스는 키가 유효해도 401을 돌려줍니다.

## 1-1. 응답 필드를 무엇과 대조했는가

"API 가 된다"와 "내가 읽는 필드가 맞다"는 다른 문제입니다. 호출이 200 으로
성공해도 필드 이름을 하나 잘못 적으면 그 값은 조용히 `None` 이 되고, 리포트에는
"데이터 없음"이 찍힙니다. **틀렸다는 신호가 어디에도 뜨지 않습니다.**

그래서 각 기관의 공식 예제 코드에서 응답 필드 목록을 뽑아 `.api_fields.json` 에
얼려 두고, `selftest` 가 매번 "내 매핑이 그 안에 있는가"를 검사합니다.
네트워크는 필요 없습니다.

| 출처 | 대조 대상 | 근거의 성격 |
|---|---|---|
| KRX 일별매매 | 실응답 대조 2026-08-13 · stk_bydd_trd basDd=20260812 (942건) | 실응답 직접 대조 (공식 예제 저장소 없음) |
| DART | PyPI OpenDartReader 0.3.3 · opendartreader/*.py | 해당 기관 API 전용 표준 클라이언트 |
| ECOS 100대 통계 | PyPI PublicDataReader 1.1.1.post2 · PublicDataReader/ecos/ecos.py | 해당 기관 API 전용 표준 클라이언트 |
| KIS 현재가 | github.com/koreainvestment/open-trading-api @main · examples_llm/domestic_stock/inquire_price/chk_inquire_price.py | 기관 공식 예제 |
| KIS 호가 | github.com/koreainvestment/open-trading-api @main · examples_llm/domestic_stock/inquire_asking_price_exp_ccn/chk_inquire_asking_price_exp_ccn.py | 기관 공식 예제 |
| FRED 관측치 | PyPI fredapi 0.5.2 · fredapi/fred.py | 해당 기관 API 전용 표준 클라이언트 |

### 매크로 캘린더 — 일정에도 등급이 있습니다

"지금이 그 때인가"(§4)는 지나간 값만으로는 답이 안 나옵니다. 이번 주에 금통위가
있는지, 파생 만기가 끼어 있는지, 락업이 풀리는지가 매도 시점을 그대로 바꿉니다.

| 등급 | 무엇 | 어디서 |
|---|---|---|
| **공표** | 금통위(연 8회) · FOMC(연 8회, SEP 표시) | 기관이 연 1회 공표 → `.calendar.json` 에 적고 **출처와 확인 날짜**를 답니다 |
| **규칙** | 지수선물·옵션 만기 (매월 둘째 목요일, 3·6·9·12월은 분기 동시만기) | 제도 규칙에서 계산 — 외부 호출 없음 |
| **추정** | 락업 만료 (상장 + 6개월·1년), 상장 3년 | 원장의 상장일에서 관행으로 계산 |

**락업은 추정입니다.** 의무보유확약이 아니고, 실제 기간·대상은 증권신고서를 봐야
합니다. 그 사실이 값에 붙어 리포트에 함께 실립니다.

금통위·FOMC 는 **공개 API 가 없습니다.** 가져올 수 없는 것을 가져온 척하지 않고,
대신 **바닥나기 전에 알려 줍니다.**

```bash
python docs/fetch_calendar.py     # 남은 일정 · 60일 미만이면 경고
python ki_monitor.py calendar --days 90
```

평일 07:30 자동 실행이 이것을 함께 돌립니다.

### 논문은 어디서 가져오는가

**네 곳**을 함께 훑습니다. 성격이 달라 **등급도 다릅니다.**

| 출처 | 무엇 | 등급 | 왜 |
|---|---|---|---|
| **arXiv q-fin** | 계량금융 프리프린트 8개 분류 | 프리프린트 (동료심사 전) | **퀀트 트레이딩 논문이 공개 API 로 가장 많이 모이는 곳.** 특히 `q-fin.TR`(거래·시장미시구조)·`q-fin.PM`(포트폴리오). 초록이 전문으로 옵니다 |
| **Semantic Scholar** | 2억+ 건 | 게재본은 저널 / SSRN·arXiv 는 프리프린트 | `externalIds` 에 **SSRN·arXiv·DOI 가 함께** 와서, 워킹페이퍼가 어느 저널로 갔는지 이어집니다 |
| **OpenAlex** | 2억 5천만 건 | 게재본은 저널 / SSRN 색인분은 프리프린트 | 폭이 가장 넓고 피인용 수가 함께 옵니다 |
| **Crossref** | 게재된 저널 논문 | 저널 게재 (동료심사) | 발행 정보가 가장 정확해 재대조(`--verify`)에 씁니다 |

**SSRN 은 퀀트 파이낸스 워킹페이퍼가 가장 많이 올라오는 곳이지만 셀프서비스
API 가 없습니다**(Elsevier). 지원팀 문의만 가능하고 개인에게 내주는 사례는
드뭅니다. 긁어 오는 것은 이용약관 위반이라 하지 않습니다 — 대신 **Semantic
Scholar 와 OpenAlex 가 색인한 SSRN 프리프린트**가 함께 들어오고, 프리프린트
등급으로 표시됩니다.

### API 키

키를 쓰는 곳은 **한 곳뿐**입니다. 나머지 셋은 키 없이 공개 API 로 받습니다.

| 항목 | 신청 | 효과 |
|---|---|---|
| `S2_API_KEY` | [무료 신청 폼](https://www.semanticscholar.org/product/api#api-key-form) | 공용 풀(429 잦음) → 전용 1 req/s |

`stock-monitor/.env` 에 적습니다. **없어도 넷 다 돕니다** — 키가 없다고 수확이
멈추지 않고, 공용 풀에서 더 천천히 돌 뿐입니다(3초 간격).

arXiv·OpenAlex·Crossref 는 익명으로 부르므로 호출 간격을 넉넉히 둡니다
(arXiv 3초, 나머지 1초). 몰아치면 막히는 쪽은 이쪽입니다.

초록을 함께 가져오는 것이 중요합니다. 제목만 보고 판단하면 심사가 "판단 불가"로
끝나고, 그러면 훑어 온 의미가 없습니다. 상한에 걸려 잘릴 때도 **초록 있는 것이
먼저 남습니다.**

프리프린트는 동료심사를 거치지 않았습니다. 결과가 뒤집히거나 게재되지 못한 채
남는 일이 흔합니다. 그래서 후보마다 등급이 붙고, 심사 프롬프트가 그것을 무시하지
말라고 명시합니다.

논문은 **연도별로** 관리합니다. 손으로 고른 목록은 고른 날짜에서 멈추기 때문입니다.

```bash
python docs/fetch_papers.py                     # 연도별 현황 · 몇 년째 갱신이 없는지
python docs/fetch_papers.py --harvest 2013-2026 # 그 구간을 훑어 후보에 쌓기
python docs/fetch_papers.py --verify            # 채택본 발행정보 재대조
```

훑어 온 것은 **후보**일 뿐 인용되지 않습니다. 이 데스크가 묻는 네 질문 중 하나를
바꿀 때만 채택본으로 옮기고, 그때 그 논문이 **주장하지 않는 것**을 함께 적습니다.

목록 갱신 (인터넷 필요):

```bash
python docs/fetch_api_spec.py
```

KRX 만 근거가 다릅니다. 이 신 Open API 전용 공식 예제도, 표준 클라이언트도
없습니다 — 널리 쓰이는 `pykrx` 는 구 `data.krx.co.kr` 화면용 엔드포인트를 쓰는
**다른 API** 입니다. 그래서 KRX 는 실제 응답을 받아 대조한 기록을 얼려 두었고,
거기서 두 가지를 검산했습니다 — 거래대금÷거래량이 고가·저가 범위 안에 들어오는지,
종가 × 상장주식수 = 시가총액 항등식이 성립하는지.

**대조했다는 것이 값이 맞다는 뜻은 아닙니다.** 이름이 맞아도 실제 값이
상식적인지는 한 번 받아 봐야 압니다. 그래서 KIS·ECOS 는 `verified=False` 로 두고,
`check-auth` 가 "명세 대조 완료 · 실응답 미대조" 라고 그대로 적습니다.

---

## 2. 의도적으로 제외한 것

아래는 FRED로 받아볼 수는 있으나 **저작권이 제3자에게 있어 기본값에서 제외**했습니다.
`macro-us --include-restricted` 로만 켜지며, 켜기 전에 해당 제공처의 조건을 확인하십시오.

| 계열 | FRED 코드 | 저작권 주체 |
|---|---|---|
| VIX | `VIXCLS` | Cboe Exchange, Inc. |
| 나스닥 종합지수 | `NASDAQCOM` | Nasdaq, Inc. |
| 미국 하이일드 스프레드 | `BAMLH0A0HYM2` | ICE Data Indices, LLC |

이미 적재된 제3자 계열은 `macro-us --purge-restricted` 로 원장에서 지울 수 있습니다.

## 3. 사용하지 않는 것

- **웹 크롤링 없음.** 어떤 사이트도 스크래핑하지 않습니다. 전부 공식 API입니다.
- **유료 벤더 데이터 없음.** 애널리스트 컨센서스·목표주가 미사용.
- **뉴스 기사 없음.** 언론사 콘텐츠를 복제하지 않습니다.
  기업 관련 사건은 DART 공시 원문으로만 표시합니다.
  공시에는 접수번호로 만든 **원문 링크**를 답니다 — 제목만으로는 발행조건·
  전환가·리픽싱 조항을 알 수 없고, 그건 1차 출처를 본 것이 아닙니다.

## 4. 생성물에 자동으로 표기되는 내용

- 출처 3곳 명기 (§ 데이터 출처)
- 제3자 저작 계열을 켠 경우 그 저작권 주체
- 투자권유·투자자문 자료가 아니라는 고지
- 배포 통제 배너 (`STAGE` 설정에 따름)

## 5. 배포 통제 스위치

`ki_monitor.py` 의 `STAGE` 값으로 제어합니다.

- `personal` (기본) — 워터마크 표시, 알림 발송 안 함
- `corporate` — `ENV["corporate"]["recipients"]` 에 수신자 기재

생성되는 리포트에는 감시 대상 기업의 실명과 처분 관련 지표가 포함됩니다.
조직에서 사용하실 경우 **데이터 라이선스보다 열람 범위 관리가 실질적으로 더 중요합니다.**
