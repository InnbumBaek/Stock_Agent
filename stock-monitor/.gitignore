# ── 자격증명 ──────────────────────────────────────────────────────
# API 키. 유출되면 재발급 외에는 방법이 없습니다.
.env
.env.*
!.env.example
env.txt
env*.txt
*.env.txt

# ── 대외비 데이터 ─────────────────────────────────────────────────
# 포트폴리오사 실명·회수계획·보유 포지션.
# 저작권이 아니라 영업비밀·미공개중요정보 문제입니다.
watchlist.csv
exit_plan.csv
positions.csv
*.sample.csv.bak

# ── 원장 · 산출물 ─────────────────────────────────────────────────
# 87MB 이고 API 로 언제든 다시 만들 수 있습니다. 버전관리 대상이 아닙니다.
*.sqlite
*.sqlite-journal
*.parquet
.corp_code_cache.parquet
out/

# ── 사내 원본 자료 ────────────────────────────────────────────────
*.pptx
*.xlsx
*.hwp

# ── 파이썬 · 주피터 ───────────────────────────────────────────────
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# ── KIS 접근토큰 캐시 ────────────────────────────────────────────
# 자격증명입니다. 하루짜리지만 있는 동안은 그대로 쓸 수 있습니다.
.kis_token.json
.api_verified.json
.papers_candidates.json
.api_verified.tmp
